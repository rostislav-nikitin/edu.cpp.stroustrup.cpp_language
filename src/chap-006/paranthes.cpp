#include <iostream>

int main()
{
	//int a = (((b + (c * d)) << 2) & 8);

	//int x = (a & (077 != 3));

	//x = ((a == b) || ((a == c) && (c < 5)));

	//int c = x! = 0;

	//0 <= i<7
	//x = (f(1, 2)) + 3;

	//int b = 10;
	//int a=-1++b--5;
	//int a=-b++;
	
	//a = (b == (c++));
	//(a = (b = (c = 0)));
	
	//((a[4])[2]) *= ((*b) ? (c) : ((*d)*2));
	
	//(((a-b),c)=d)
	


	//std::cout << a << std::endl;

	return 0;
}
