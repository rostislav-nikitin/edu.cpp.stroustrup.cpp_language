#include <string>
#include "date.h"

namespace DateTime
{
	int days_in_month(Date::Month month, bool leap_year);
	int days_in_year(int year);
	bool leap(int year);

	std::string to_string(Date &date);
	std::string to_string(Date::Month month);
}
